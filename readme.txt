1) Primer paso des cargar el proyecto y descomprimir el archivo de trabajo y ejecuarlo en Visual Studio Code,
después abrir la terminal y posteriormente ejecutar estas dependencias en orden:
I) npm init -y
 II) npm install express bcryptjs
   III) cookie-parser jsonwebtoken 
    IV) npm install --save -dev nodemon
2)Para ejecutar el proyecto hay que colocar en la terminal : node ./server.js 
despues usted va hacia el explorador de preferencia y pone en el buscador "localhost:4000"
en esa pestaña dara a un html configurado con back-end el cual pedira un inicio de seccion y abajo un registro,
al iniciar sesion sin algun usuario registrado desde el network el cual puede acceder desde el F12 dara un aviso de este,
depues si crea un usuario e intenta iniciar sesion nuevamente, ingresara directamente al html admin el cual contine cookie-parse ademas de secciones de venta,
3)El codigo tiene una encriptacion de clave ademas de un registro el cual se mostrara en la terminal del visual studio junto a su cookie la cual tiene una fecha 
de expiro
🤑🤑🤑🤑🤑🤑🙄🤣👍👍👍👍👌